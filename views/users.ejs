<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title>UserGram</title>
  </head>
  <style>
    #add {
      color: white;
      border: 2px solid gray;
      padding: 0.5rem 1rem;
      font-size: 1.5rem;
      margin: 3rem;
      font-weight: 600;
      display: inline-block;
    }
  </style>
  <body>
    <a id="add" href="http://localhost:3000/users/add">ADD USER</a>
    <div class="container">
      <%for(user of users) {%>
      <div>
        <div class="username"><%=user.username%></div>
        <div class="profile">
          <img src="<%=user.profile%>" alt="dummy" />
        </div>
        <div class="actions">
          <a href="http://localhost:3000/users/<%=user.user_id%>/edit">Edit</a>
          <form method="post" action="/users/<%=user.user_id%>?_method=DELETE">
            <button>Delete</button>
          </form>
          <a href="http://localhost:3000/users/<%=user.user_id%>/view">View</a>
        </div>
      </div>
      <%}%>
    </div>
  </body>
</html>
